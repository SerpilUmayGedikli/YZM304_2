class CIFAR10_VGG16(nn.Module):
    def __init__(self):
        super(CIFAR10_VGG16, self).__init__()
        self.vgg = models.vgg16(pretrained=False)
        self.vgg.classifier[6] = nn.Linear(4096, 10) 

    def forward(self, x):
        return self.vgg(x)

model = CIFAR10_VGG16().to(device)
